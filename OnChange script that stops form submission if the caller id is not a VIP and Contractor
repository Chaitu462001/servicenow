SCRIPT INCLUDE : 
var FormValidation = Class.create();
FormValidation.prototype = {
    initialize: function() {},

    validateForm: function(vip, contractor) {
        if (vip === true && contractor === true) {
            return true;
        } else {
            return false;
        }
    },

    type: 'FormValidation'
};

CLIENT SCRIPT :
function onSubmit() {
    var vipField = g_form.getValue('vip');
    var contractorField = g_form.getValue('contractor');

    if (vipField === 'true' && contractorField === 'true') {
        // Allow form submission
        return true;
    } else {
        g_form.addErrorMessage("Both VIP and Contractor must be selected as true to submit this form.");
        return false;
    }
}
